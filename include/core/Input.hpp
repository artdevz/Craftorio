#pragma once

namespace Input {
    void Update();
    bool IsMovingUp();
    bool IsMovingLeft();
    bool IsMovingDown();
    bool IsMovingRight();
    bool IsRunning();
    bool IsOpeningInventory();
    bool IsOpeningMenu();
}